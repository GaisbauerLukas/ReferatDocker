# Image Pull
docker pull {imageName}
# Dies ist im Normalfall aber nicht notwendig da beim starten eines Containers das Image, 
# solang es nicht vorhanden ist, automatisch heruntergeladen wird

# Image Push
docker push {imageName}

# Login to DockerHub
docker login

# Ausloggen
docker logout